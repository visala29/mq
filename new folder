pipeline {
    agent any
    
    stages {
        stage('Create Folder on C Drive') {
            steps {
                script {
                    def folderPath = "C:\\my-folder"  // Specify the folder path on C drive
                    bat "powershell.exe New-Item -ItemType Directory -Path ${folderPath}"
                }
            }
        }
    }
}
